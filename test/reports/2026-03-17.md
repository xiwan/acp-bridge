# ACP Bridge 测试报告 — 2026-03-17

## 版本

- **ACP Bridge**: v0.7.0
- **测试时间**: 2026-03-17 15:21 UTC

## 测试环境

- OS: Amazon Linux (EC2)
- Agents: kiro (ACP), claude (ACP), codex (PTY, disabled — LiteLLM not running)
- OpenClaw Gateway: http://10.0.1.79:18789

## 测试结果

| 套件 | 通过 | 失败 | 说明 |
|------|------|------|------|
| 公共 | 3 | 1 | codex 未启用导致 agent 列表检查失败 |
| Tools Proxy | 9 | 0 | ✅ 全部通过 |
| Kiro | 7 | 0 | ✅ 同步/流式/多轮/异步全部通过 |
| Claude | 2 | 0 | ✅ 同步/流式通过 |
| Codex | 0 | 3 | ❌ LiteLLM 未运行，codex agent 被禁用 |
| **合计** | **21** | **4** | |

## v0.7.0 新功能测试

### Tools Proxy (9/9 ✅)

- [x] `GET /tools` 返回 tool 列表
- [x] tool 列表包含 message/tts/web_search/nodes
- [x] `GET /tools` 包含 openclaw_url
- [x] `POST /tools/invoke` message send 成功（Discord 实际收到消息）
- [x] 空 tool name 返回错误
- [x] `tools/tools-client.sh` 客户端正常工作

### Webhook 默认值

- [x] job 提交不传 `account_id` / `discord_target` 自动使用 config 默认值
- [x] `/tools/invoke` 不传 `accountId` 自动注入默认值
- [x] Discord 消息实际收发验证通过

### 已知问题

1. **Codex 不可用**: LiteLLM proxy 未启动，codex agent 自动禁用（预期行为）
2. **公共测试 codex 检查失败**: codex 被禁用后不在 agent 列表中（预期行为）
3. **TTS**: 生成成功但音频文件在 OpenClaw 远端机器上，无法通过 HTTP API 自动投递

## 变更摘要

- 新增 `GET /tools` + `POST /tools/invoke` 端点（OpenClaw tools 代理）
- 新增 `tools/tools-client.sh` 独立客户端
- 新增 `config.yaml` webhook 默认值（`account_id`, `discord_target`）
- 新增 `test/test_tools.sh` 测试套件（9 个用例）
- `skill/acp-client.sh` 保持纯 agent 客户端定位
