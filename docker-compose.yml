services:
  acp-bridge:
    build: .
    ports:
      - "8001:8001"
    environment:
      - ANTHROPIC_API_KEY
      - ACP_BRIDGE_TOKEN
      - LITELLM_API_KEY
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./workspace:/workspace
    restart: unless-stopped
